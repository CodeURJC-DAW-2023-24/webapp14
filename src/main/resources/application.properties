# Configuración del servidor
server.port = 8443
server.ssl.key-store = classpath:keystore.jks
server.ssl.key-store-password = password
server.ssl.key-password = secret

# Configuración de Mustache
spring.mustache.suffix = .html

# Configuración de logging
logging.level.org.springframework.web = DEBUG

# Configuración de la base de datos
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/ticketeventaplicacion?serverTimezone=Europe/Madrid
spring.datasource.username = root
spring.datasource.password = papr0bar
spring.sql.init.mode = never

# Configuración de JPA
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true

# Configuración de Hibernate para no recrear las tablas en cada inicio
spring.jpa.hibernate.ddl-auto = update